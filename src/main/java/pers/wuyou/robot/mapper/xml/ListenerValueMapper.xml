<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pers.wuyou.robot.mapper.ListenerValueMapper">

    <select id="getDefaultValue" resultType="java.lang.String">
        select value from listener_value join listener_values lv on listener_value.id = lv.value_id
        <where>
            name = #{name} and listener_id = #{id}
        </where>
    </select>
</mapper>
